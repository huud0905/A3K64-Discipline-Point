/* Disabled temporarily.
   The previous DOM controller caused a render loop/freezing on /desktop/messages.
   Keep this file as a no-op so existing imports remain safe while the mobile Messages UI is rebuilt in React properly. */

export {};
